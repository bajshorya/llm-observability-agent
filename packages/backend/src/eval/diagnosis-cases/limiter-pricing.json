{
  "name": "limiter-pricing",
  "scenario": "limiter-misconfig",
  "capturedAt": "2026-09-05T03:22:31.029Z",
  "sha": "0c701a0bcc1a3156a039260f598537731b16f51e",
  "expect": {
    "explainsTheFailure": false,
    "note": "A price-formatting change on the read path. It touches nothing on the write path and cannot cause quota rejections"
  },
  "commitFiles": [
    "src/lib/pricing.js",
    "src/routes/orders.js"
  ],
  "context": "Service: orders-api\nWindow: 2026-08-16T18:57:00.000Z to 2026-08-16T19:02:00.000Z (5 min)\nSeverity: medium\nAffected area: orders-api\n\nWhat the classification stage concluded:\n  Stub classification for orders-api: 1 Tier 1 signal(s) fired (new_error_signature). Severity is scored from which detectors tripped — no model was called. Set LLM_PROVIDER to classify for real.\n\nWhat the detectors found:\n- new_error_signature: \"Rate limiter rejecting <num>% of write requests across <num> distinct clients in the last minute (burst <num>, refill <num>/s)\" occurred 5 times and appears nowhere in the baseline hour\n    raw example: Rate limiter rejecting 40% of write requests across 1557 distinct clients in the last minute (burst 120, refill 2/s)\n\nWhat the correlation stage concluded:\n  Commit 0c701a0bcc (confidence 0.25)\n  Files implicated: (none named)\n  Reasoning: Stub correlation: this is simply the most recent commit in the candidate list — the naive 'blame the last deploy' heuristic, applied without reading anything. No model was called, and no mechanism was checked. Set LLM_PROVIDER to correlate for real.\n\nThe commit, in full:\n\n  0c701a0bcc  2026-08-16 17:25Z  —  orders-api ci\n    feat(pricing): show the promotional total on order responses\n      Adds discounted_total to every order response so the storefront can strike\n      through the original price without a second call.\n    files (2):\n      src/lib/pricing.js  +7/-1\n      src/routes/orders.js  +2/-1\n\n    diff:\n      diff --git a/src/lib/pricing.js b/src/lib/pricing.js\n      index ac0271a..246f33e 100644\n      --- a/src/lib/pricing.js\n      +++ b/src/lib/pricing.js\n      @@ -4,4 +4,10 @@ function formatPrice(cents) {\n         return (cents / 100).toFixed(2);\n       }\n       \n      -module.exports = { formatPrice };\n      +// Promotional pricing: an order carries the discounted total once a promotion\n      +// has been applied to it.\n      +function formatDiscountedPrice(order) {\n      +  return formatPrice(order.discounted_cents);\n      +}\n      +\n      +module.exports = { formatPrice, formatDiscountedPrice };\n      diff --git a/src/routes/orders.js b/src/routes/orders.js\n      index f7dd86b..f5c6d52 100644\n      --- a/src/routes/orders.js\n      +++ b/src/routes/orders.js\n      @@ -1,6 +1,6 @@\n       const { Router } = require(\"express\");\n       const db = require(\"../lib/db\");\n      -const { formatPrice } = require(\"../lib/pricing\");\n      +const { formatPrice, formatDiscountedPrice } = require(\"../lib/pricing\");\n       \n       const router = Router();\n       \n      @@ -9,6 +9,7 @@ function present(order) {\n           ...order,\n           created_at: new Date(order.created_at).toISOString(),\n           total: formatPrice(order.total_cents),\n      +    discounted_total: formatDiscountedPrice(order),\n         };\n       }"
}
